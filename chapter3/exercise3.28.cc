/*
What are the values in the following arrays?
```
string sa[10];
int ia[10];
int main() {
    string sa2[10];
    int ia2[10];
}
```
*/

// sa[10] is default initialized with global scope and has elements of value "".
// ia[10] is default initialized with global scope and has elements of value 0.

// sa2[10] is default initialized with local scope and has elements with undefined value.
// ia2[10] is default initialized with local scope and has elements with undefined value.
