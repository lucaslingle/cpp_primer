/*
List some drawbacks of using an array instead of a vector.
*/

// an array has fixed size.
// an array uses base types' default initialization, 
//    so if defined inside a function it will have undefined values unless initialized.
// an array does not support assignment or copy initialization from other arrays. 
