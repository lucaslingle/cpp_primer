/*
Explain the following expression:
```
double slope = static_cast<double>(j/i);
```
*/

// assuming i and j are ints, it divides i by j and removes the remainder,
// returning the result as an int, which is then cast to a double by static_cast.
