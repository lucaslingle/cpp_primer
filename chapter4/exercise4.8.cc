/*
Explain what operands are evaluated in the 
logical AND, logical OR, and equality operators.
*/

// logical AND: left operand is evaluated, 
// and if true, right operand is evaluated.

// logical OR: left operand is evaluated,
// and if false, right operand is evaluated.

// equality operator: both sides are evaluated
// after being converted to the same type. 
