/*
Given the variable definitions in this section, explain what conversions
take place in the following expressions:
(a) if (fval)
(b) dval = fval + ival
(c) dval + ival * cval

Remember that you may need to consider the associativity of the operators.
*/

// (a) fval from float to bool.
// (b) ival from int to float, (fval + ival) to double.
// (c) cval from char to int (integral promotion), and (ival * cval) to double.
