/*
Name two type conversions allowed on function arguments
involved in template argument deduction.
*/

// const conversions: a non-const argument can be converted to a const parameter.
// array/function pointer conversions: arrays and functions are converted
//     to pointers to the first element and to the function type, respectively.
