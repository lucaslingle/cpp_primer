/*
Contrast the following program with the one in the previous exercise:
```
vector<int> v;
v[0] = 1;
```
*/

// an empty vector of ints is defined.
// an out-of-bounds index 0 is used to set an element to 1.
// this results in undefined behavior.

// in contrast to the previous exercise using map,
// no element is automatically inserted when using subscripting.
