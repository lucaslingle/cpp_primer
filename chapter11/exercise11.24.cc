/*
What does the following program do?
```
map<int, int> m;
m[0] = 1;
```
*/

// creates a map m, from int keys to int values.
// inserts a new pair into m, initialized to (0, 0). 
//     the key is specified as 0, and value initialization is used for the mapped value.
// fetches the mapped value for key 0 as an lvalue, and sets it to 1.
