/*
Describe the differences between a map and a vector.
*/

// a vector is indexed by position and elements must be explicitly added.
// a map is indexed by a key and elements are automatically created 
//     the first time a key is used.
