/*
What does the following program do?
```
int i = 42;
int *p1 = &i;
*p1 = *p1 * *p1;
```
*/

// it sets the variable i to 42,
// it makes the pointer p1 store the address where i is,
// and changes the value stored in i to be the old value of i squared.
