/*
Would it be legal for both the constructor that takes a string
and the one that takes an istream& to have default arguments?
If not why not?
*/

// not it is not.
// it is not possible to tell these functions apart 
// when they are both called with default arguments.
