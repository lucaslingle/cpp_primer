/*
How does the `protected` access specifier differ from `private`?
*/

// private data members and member functions are visible to other instances 
// of the same class, but not to other classes.

// protected data members and member functions are visible to derived classes.