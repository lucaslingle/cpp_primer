/*
Define static type and dynamic type.
*/

// static type is the type of an object at compile time
// dynamic type is the type of an object at runtime

// for a pointer or reference to a class type, the dynamic type
// can differ from the static type due to inheritance.