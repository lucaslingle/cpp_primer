/*
Can a container have a capacity less than its size?
*/

// no. the capacity is the number of stored elements allowed 
// before a reallocation is required; the size is the current
// number of stored elements. 
