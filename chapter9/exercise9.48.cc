/*
Given the definition of ```name``` and ```numbers``` on p. 365,
what does ```numbers.find(name)``` return?
*/

// name is defined as either "AnnaBelle" or "r2d2",
// while numbers is "0123456789".
// in either case, the name string is not a substring of the numbers string,
// so the return value is string::npos.
