/*
Why didn't we use in-class initializers in PersonInfo?
*/

// the default initialization for the string and vector data members are
// empty string and empty vector, respectively. these are perfect data values
// especially for an aggregate class, where the class user can set them as required.
