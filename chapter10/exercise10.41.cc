/*
Based only on the algorithm and argument names, describe the operation that
each of the following library algorithms performs:
```
replace(beg, end, old_val, new_val);
replace_if(beg, end, pred, new_val);
replace_copy(beg, end, dest, old_val, new_val);
replace_copy_if(beg, end, dest, pred, new_val);
```
*/

// replace(beg, end, old_val, new_val)
// replaces each occurrence of old_val by new_val.

// replace_if(beg, end, pred, new_val)
// replaces each occurrence where pred(val) is true with new_val.

// replace_copy(beg, end, dest, old_val, new_val)
// copies the range [beg, end), replacing each occurrence of old_val by new_val. 

// replace_copy_if(beg, end, dest, pred, new_val);
// copies the range [beg, end), each occurrence where pred(val) is true with new_val.
